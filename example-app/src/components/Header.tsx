import { ReactElement } from 'react';

function Header(): ReactElement {
  return (
    <div style={{ paddingTop: '50px', height: '100px' }}>
      <p>So cooewl website</p>
    </div>
  );
}

export default Header;
